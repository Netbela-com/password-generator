---
apiVersion: v1
kind: Service
metadata:
  name: netbela-password-generator
  namespace: netbela
spec:
  selector:
    app: netbela-password-generator
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP